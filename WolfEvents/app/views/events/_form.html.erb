<%= form_with(model: event) do |form| %>
  <% if event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(event.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
        <% event.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>


  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :category %>
    <%= form.collection_select :category, ['Concerts', 'Sports', 'Arts & Theatre', 'Miscellaneous/Family – Private'], :to_s, :to_s, prompt: 'Select category' %>
  </div>

  <div class="field">
    <%= form.label :date %>
    <%= form.text_field :date, class: "datepicker", type: "date" %>
  </div>

  <div class="field">
    <%= form.label :startTime %>
    <%= form.time_field :startTime, class: "timepicker", type: "time" %>
  </div>

  <div class="field">
    <%= form.label :endTime %>
    <%= form.time_field :endTime, class: "timepicker", type: "time" %>
  </div>

  <div class="field">
    <%= form.label :ticketPrice %>
    <%= form.text_field :ticketPrice %>
  </div>

  <div class="field">
    <%= form.label :seatsLeft %>
    <%= form.number_field :seatsLeft %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
